# Sample Workspace Fixture

Synthetic, deterministic mini-repository used by automated tests. Tests must copy this
fixture into a unique temporary directory (see `SharpAgent.TestKit.Workspaces.TempWorkspace`)
and never operate on real developer checkouts.

This file intentionally contains no secrets, credentials, or private data.
